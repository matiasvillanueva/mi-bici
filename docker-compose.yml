services:
  collab:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "8888:8888"
    command: ["python3", "/workspace/scripts/start_collab.py", "/workspace"]
