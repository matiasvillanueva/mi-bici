services:
  collab:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "8888:8888"
    command: ["bash", "/workspace/scripts/start_collab.sh", "/workspace"]
